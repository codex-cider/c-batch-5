#include <stdio.h>
void main()
{

    int a[10];
    int i;

    for (i = 0; i < 10; i++)
    {
        printf(" enter the value of a[%d]:\n", i);
        scanf("%d", &a[i]);
    }
    int f;
    printf("enter the number for finding:\n");
    scanf("%d", &f);

    for ()
    {
        printf("number is found ");
    }
}