#include <stdio.h>
void main()
{
    int number;
    printf("Enter any number between 1 to 10 : ");
    scanf("%d", &number);

    if (number >= 10 || number < 1)
    {
        printf("number is invalid", number);
    }
    else if
    {
        printf("%d is incorrect", number);
    }
}



